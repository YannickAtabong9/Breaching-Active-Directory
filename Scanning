#Nmap

nmap -sn target.com                # Ping scan
nmap -sS target.com                # SYN scan
nmap -sV target.com                # Service version detection
nmap -O target.com                 # OS detection
nmap -A target.com                 # Aggressive scan
nmap -p 22,80,443 target.com       # Scan specific ports
nmap -p- target.com                # Scan all ports
nmap --script vuln target.com      # Script scanning


#Masscan

masscan target.com -p1-65535 --rate=1000


#Unicornscan

unicornscan -i eth0 -mT target.com:a



#RustScan

rustscan -a target.com
